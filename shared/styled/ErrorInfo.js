import styled from 'styled-components'

export const ErrorInfo = styled.div`
  background-color: rgba(248, 95, 115, 0.06);
  border-radius: 0.32rem;
  color: rgba(248, 95, 115, 1);
  padding: 1rem;
  text-align: center;
  position: relative;
  z-index: 1;

  small {
    font-size: 0.82rem;
    opacity: 0.62;
  }
`
